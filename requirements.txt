# Core dependencies for Pipeline Verification System
# Python 3.11+ required

# For testing
pytest>=7.0.0
pytest-cov>=4.0.0

# For configuration and data handling
PyYAML>=6.0

# For CLI argument parsing (built-in, but listed for clarity)
# argparse is built-in to Python standard library

# For JSON handling (built-in)
# json is built-in to Python standard library

# For file system operations (built-in)
# pathlib is built-in to Python standard library